#!/usr/bin/env bash
set -euo pipefail

BACKUP_DIR="${BACKUP_DIR:-/var/backups/renpia}"
TIMESTAMP="$(date +%Y%m%d_%H%M%S)"

mkdir -p "$BACKUP_DIR"
pg_dump "$DATABASE_URL" | gzip > "$BACKUP_DIR/renpia_${TIMESTAMP}.sql.gz"

find "$BACKUP_DIR" -type f -name "renpia_*.sql.gz" -mtime +30 -delete
